# Keep requirements + plans in git; ignore loop exhaust.

# Run artifacts can be large/noisy.
/runs/

# Deterministic verifier logs.
/state/verify/
/state/verify-llm/

# Precomputed git snapshots.
/state/git.*
